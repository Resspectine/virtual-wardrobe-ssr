import { Box, styled } from '@mui/system';

export const TagsWrapper = styled(Box)({
  display: 'flex',
  marginTop: 16,
  marginLeft: -5,
  marginRight: -5,
  marginBottom: -10,
  flexWrap: 'wrap',
});
