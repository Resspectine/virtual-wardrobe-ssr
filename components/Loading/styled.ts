import { Box, styled } from '@mui/material';

export const LoadingWrapper = styled(Box)({
  width: '100%',
  height: '100%',
  top: 0,
  left: 0,
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
});
