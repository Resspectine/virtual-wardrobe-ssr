import { Autocomplete } from '@mui/material';
import { styled } from '@mui/system';

export const TagAutocompleteStyled = styled(Autocomplete)({
  width: '50%',
  margin: '10px 0',
}) as typeof Autocomplete;
